import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  button: {
    flexDirection: 'row',
    backgroundColor: '#000',
    height: 56,
    padding: 10,
    borderRadius: 5,
    alignItems: 'center',
    justifyContent: 'center',
    gap: 12
  },

  text: {
    color: '#fff',
    fontWeight: 'bold',
    fontSize: 18
  },
})